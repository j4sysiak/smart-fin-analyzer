# ==================================================
# PROFIL 'tc' ? Testcontainers PostgreSQL
# ==================================================
# Datasource jest ustawiany dynamicznie przez BaseIntegrationSpec
# (@DynamicPropertySource), wi?c nie definiujemy tu URL, user, password.

# Flyway W??CZONE dla profilu 'tc' ? pozwala wykona? migracje DB w kontenerze testowym
# W testach uruchamianych z Testcontainers chcemy, aby Flyway wykona? skrypty migracyjne
# (w przeciwnym razie Hibernate tworzy schemat i mog? brakowa? migracji). Je?li baza
# nie jest pusta mo?na doda? baseline-on-migrate=true ? pozostawiamy je w??czone z ostro?no?ci?.
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Logi SQL wy??czone (w??cz na true do debugowania)
spring.jpa.show-sql=false

# Wy??czamy schedulery na czas testów
app.scheduling.enabled=false
spring.task.scheduling.enabled=false

# Pozwalamy nadpisywa? beany (ró?ne konfiguracje testowe)
spring.main.allow-bean-definition-overriding=true

logging.level.org.flywaydb=DEBUG