Lab82
-----

Lab82--JPA-Relationships--Relacje-Many-to-One-i-problem-N+1
===========================================================
>>>>
Cześć [imię],

Potrzebuję Twojej pomocy przy dodaniu testowych danych do OpenBao. Projekt nad którym pracuję wymaga integracji z OpenBao w celu zarządzania hasłami PKI, a na tym etapie musimy ręcznie utworzyć odpowiedni wpis – zgodnie z wytycznymi od [Ważniaka z IT].

**Kontekst:**
- Playbook, który wdrażam, będzie automatycznie generował hasło PKI i zapisywał je do OpenBao za pomocą modułu Ansible (`vault_kv2_write`).
- Hasło będzie następnie odczytywane w roli `project.ait.ait_lib_generate_pkcs12` za pomocą lookupu (`vault_kv2_get`).

**Co potrzebuję zrobić teraz:**
- Utworzyć ręcznie hasło testowe na potrzeby integracji i developmentu.

**Szczegóły:**
1. **Engine:** kv2-apps-ait (należy podać przy `engine_mount_point`)
2. **Ścieżka (topic):** testclient
3. **Klucz (Key):** pkcs12_container_password
4. **Wartość:** Dowolne testowe hasło (np. `TestClientPKI123!`).

**Przykład struktury w OpenBao:**

Engine: kv2-apps-ait └── Topic: testclient └── Key: pkcs12_container_password = <testowe hasło>


Gdy hasło zostanie dodane, będę mógł przetestować proces odczytu w playbooku i upewnić się, że integracja działa zgodnie z założeniami.

Jeśli potrzebujesz więcej szczegółów lub trzeba coś doprecyzować – daj znać.

Z góry dzięki za pomoc!

Pozdrawiam,  
[jak się podpisać]


Po ręcznym dodaniu hasła od admina → możesz przetestować playbook:
vault_kv2_get w vm_configuration.yml (lookup hasła z OpenBao).
Jak admin coś odpisze (np. inne szczegóły API lub problemy z dostępem), daj znać — pomożemy to dostosować.
>>>>


Mamy już system, który jest bezpieczny, wydajny i audytowalny. 
To świetny fundament. 
Jednak w rzeczywistych systemach dane rzadko są "płaskie". 
Obecnie Twoja kategoria to tylko zwykły napis (String). 
W świecie Mid-level, dane powinny mieć swoją strukturę i relacje.

Wchodzimy w Etap 3.5: Zaawansowane Modelowanie i Relacje (JPA Relationships).

Problem: 
--------
Chcemy, aby każda kategoria miała swój limit wydatków (budżet). 
Jeśli kategoria to tylko String, nie mamy gdzie zapisać tego limitu. 
Musimy stworzyć osobną tabelę `categories` i połączyć ją z `transakcjami`.

Cel:
----
Stworzenie relacji między `TransactionEntity` a `CategoryEntity`.
Implementacja logiki sprawdzającej, czy transakcja nie przekracza budżetu kategorii.

Zrozumienie problemu N+1 – najczęstszego powodu, dla którego aplikacje Springowe zwalniają na produkcji.

Krok-1. Nowa migracja Flyway (V8__create_categories_table.sql)
--------------------------------------------------------------
Musimy stworzyć tabelę kategorii i powiązać ją z transakcjami.

`src/main/resources/db/migration/V8__create_categories_table.sql`

```sql
CREATE TABLE categories (
id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
name VARCHAR(50) NOT NULL UNIQUE,
monthly_limit DECIMAL(19, 2) NOT NULL
);

-- Wstawiamy przykładowe limity
INSERT INTO categories (name, monthly_limit) VALUES ('Jedzenie', 1500.00);
INSERT INTO categories (name, monthly_limit) VALUES ('Paginacja', 500.00);
INSERT INTO categories (name, monthly_limit) VALUES ('Praca', 3000.00);
INSERT INTO categories (name, monthly_limit) VALUES ('Wycieczka', 3500.00);
INSERT INTO categories (name, monthly_limit) VALUES ('Zakupy', 4000.00);

-- Modyfikujemy tabelę transakcji, aby wskazywała na ID kategorii zamiast String
ALTER TABLE transactions ADD COLUMN category_id BIGINT;
ALTER TABLE transactions ADD CONSTRAINT fk_category FOREIGN KEY (category_id) REFERENCES categories(id);

-- Opcjonalnie: Przypisujemy istniejące transakcje do kategorii 'Jedzenie' (aby nie psuć danych)
UPDATE transactions SET category_id = (SELECT id FROM categories WHERE name = 'Jedzenie') WHERE category = 'Jedzenie';
UPDATE transactions SET category_id = (SELECT id FROM categories WHERE name = 'Paginacja') WHERE category = 'Paginacja';
UPDATE transactions SET category_id = (SELECT id FROM categories WHERE name = 'Praca') WHERE category = 'Praca';
UPDATE transactions SET category_id = (SELECT id FROM categories WHERE name = 'Wycieczka') WHERE category = 'Wycieczka';
UPDATE transactions SET category_id = (SELECT id FROM categories WHERE name = 'Zakupy') WHERE category = 'Zakupy';
```


Krok-2. Model Kategorii (CategoryEntity.groovy)
-----------------------------------------------
`src/main/groovy/pl/edu/praktyki/repository/CategoryEntity.groovy`

```groovy
package pl.edu.praktyki.repository

import jakarta.persistence.*
import groovy.transform.Canonical

@Entity
@Table(name = "categories")
@Canonical
class CategoryEntity {
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
Long id

    @Column(unique = true, nullable = false)
    String name

    @Column(nullable = false)
    BigDecimal monthlyLimit
}
```

Krok-3. Aktualizacja TransactionEntity.groovy (Relacja)
-------------------------------------------------------
Zamieniamy String category na obiekt CategoryEntity.

```groovy
// ...
// ZAMIAST: String category
// STOSUJEMY:
@ManyToOne(fetch = FetchType.LAZY) // LAZY jest kluczowe dla wydajności!
@JoinColumn(name = "category_id")
CategoryEntity category
// ...
```

Krok-4. Serwis z walidacją budżetu (BudgetService.groovy)
---------------------------------------------------------
Nauczymy system sprawdzać, czy nowa transakcja nie "rozsadzi" budżetu.

```groovy
package pl.edu.praktyki.service

import org.springframework.stereotype.Service
import pl.edu.praktyki.repository.TransactionRepository
import pl.edu.praktyki.repository.CategoryEntity
import groovy.util.logging.Slf4j

@Service
@Slf4j
class BudgetService {
@Autowired TransactionRepository txRepo

    boolean isOverBudget(CategoryEntity category, BigDecimal newAmount) {
        // Sumujemy wydatki z bazy dla tej kategorii w tym miesiącu
        def currentSpending = txRepo.findByCategory(category)
                .findAll { it.date.month == java.time.LocalDate.now().month }
                *.amountPLN.sum() ?: 0.0

        return (currentSpending.abs() + newAmount.abs()) > category.monthlyLimit
    }
}
```

Krok-5. Test Spock – "Problem N+1" i Relacje (CategorySpec.groovy)
------------------------------------------------------------------
To jest test, w którym dowiesz się, jak Hibernate potrafi "zabić" bazę danych tysiącem zapytań i jak temu zapobiec.

Ten test jest kluczowy, ponieważ weryfikuje nie tylko czy dane się zapisują, 
ale przede wszystkim czy relacja między tabelami została poprawnie skonfigurowana w Hibernate.
`src/test/groovy/pl/edu/praktyki/repository/CategorySpec.groovy`

```groovy
package pl.edu.praktyki.repository

import org.springframework.beans.factory.annotation.Autowired
import org.springframework.transaction.annotation.Transactional
import pl.edu.praktyki.BaseIntegrationSpec
import pl.edu.praktyki.service.BudgetService
import java.time.LocalDate

class CategorySpec extends BaseIntegrationSpec {

   @Autowired TransactionRepository transactionRepository
   @Autowired CategoryRepository categoryRepository
   @Autowired BudgetService budgetService

   def setup() {
      // Czyścimy w odpowiedniej kolejności ze względu na klucze obce!
      transactionRepository.deleteAll()
      categoryRepository.deleteAll()
   }

   def "powinien poprawnie zapisać transakcję z powiązaną kategorią"() {
      given: "nowa kategoria z limitem"
      def foodCategory = new CategoryEntity(name: "Jedzenie", monthlyLimit: 1000.0)
      categoryRepository.save(foodCategory)

      and: "nowa transakcja przypisana do tej kategorii"
      def tx = new TransactionEntity(
              originalId: "REL-1",
              amount: 150.0,
              amountPLN: 150.0,
              category: foodCategory, // Przekazujemy obiekt, a nie String!
              date: LocalDate.now()
      )

      when: "zapisujemy transakcję"
      transactionRepository.save(tx)

      then: "możemy odczytać nazwę kategorii bezpośrednio z obiektu transakcji"
      def savedTx = transactionRepository.findAll()[0]
      savedTx.category.name == "Jedzenie"
      savedTx.category.monthlyLimit == 1000.0
   }

   def "powinien wykryć przekroczenie budżetu w kategorii"() {
      given: "kategoria z małym limitem"
      def funCategory = new CategoryEntity(name: "Rozrywka", monthlyLimit: 200.0)
      categoryRepository.save(funCategory)

      and: "istniejący wydatek w tej kategorii (150 PLN)"
      transactionRepository.save(new TransactionEntity(
              originalId: "OLD-1",
              amountPLN: -150.0,
              category: funCategory,
              date: LocalDate.now()
      ))

      expect: "nowy wydatek na 100 PLN przekroczy budżet (150 + 100 > 200)"
      budgetService.isOverBudget(funCategory, -100.0) == true

      and: "mały wydatek na 10 PLN nie przekroczy budżetu"
      budgetService.isOverBudget(funCategory, -10.0) == false
   }

   @Transactional // Transactional jest wymagany, aby leniwe ładowanie (LAZY) zadziałało w teście
   def "demonstracja problemu N+1 (logika Mida)"() {
      given: "kilka transakcji w różnych kategoriach"
      def cat1 = categoryRepository.save(new CategoryEntity(name: "C1", monthlyLimit: 100))
      def cat2 = categoryRepository.save(new CategoryEntity(name: "C2", monthlyLimit: 100))

      transactionRepository.save(new TransactionEntity(originalId: "T1", category: cat1, amountPLN: 10, date: LocalDate.now()))
      transactionRepository.save(new TransactionEntity(originalId: "T2", category: cat2, amountPLN: 20, date: LocalDate.now()))

      when: "pobieramy wszystkie transakcje"
      def allTransactions = transactionRepository.findAll()

      then: "wyciągnięcie nazwy kategorii dla każdej transakcji może spowodować dodatkowe zapytania"
      allTransactions.each { tx ->
         println "Transakcja ${tx.originalId} należy do kategorii: ${tx.category.name}"
      }
      allTransactions.size() == 2
   }
}
```

Dlaczego to jest poziom Mid? (Wiedza na rozmowę)

Rekruter zapyta: "Co to jest problem N+1 w JPA?"
Twoja odpowiedź po tym Labie:

"To sytuacja, w której pobieramy listę N transakcji jednym zapytaniem, a następnie Hibernate wykonuje N dodatkowych zapytań do bazy, aby pobrać kategorię dla każdej transakcji z osobna. Rozwiązuję to stosując JOIN FETCH w zapytaniu JPQL lub używając EntityGraph, aby pobrać transakcje i ich kategorie jednym strzałem."

Zadanie dla Ciebie:

Wdroż migrację V8 i nową encję.

Zaktualizuj relację w TransactionEntity.

Popraw TransactionController, aby przy mapowaniu na DTO brał nazwę kategorii z obiektu entity.category.name.

Uwaga: To zadanie wymusi na Tobie poprawki w kilku miejscach (zmienił się typ kategorii ze String na Object). To najlepsza nauka refaktoryzacji.

Daj znać, jak Postgres przyjmie nową tabelę kategorii! 🏛️📈✅

