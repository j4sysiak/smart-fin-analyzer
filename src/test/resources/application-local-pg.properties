# ======================================================================================
# PROFIL: local-pg - R?czny kontener PostgreSQL TYLKO do inspekcji co dzieje w Dockerze
# ======================================================================================
# U?ycie:
# docker run -d --name smartfin-postgres \
#   -e POSTGRES_DB=smartfin_db -e POSTGRES_USER=smartfin -e POSTGRES_PASSWORD=smartfin123 \
#   -p 5432:5432 postgres:16-alpine
#
# Pol?czenie z psql:
#   docker exec -it smartfin-postgres psql -U smartfin -d smartfin_db
#
#  tutaj szczegó?y odpalania bazy PostgreSQL:
#     C:\dev\smart-fin-analyzer\src\test\groovy\pl\edu\praktyki\repository\IntegrationDbSpec.groovy


spring.datasource.url=jdbc:postgresql://localhost:5432/smartfin_db
spring.datasource.username=smartfin
spring.datasource.password=smartfin123
spring.datasource.driverClassName=org.postgresql.Driver

spring.flyway.enabled=false

# Wy??cz automatyczne czyszczenie przy zamykaniu,
# Testcontainers i tak wywali ca?y kontener, wi?c nie musimy czy?ci? tabel
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true
# spring.h2.console.enabled=false