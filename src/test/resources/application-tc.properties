# ==================================================
# KONFIGURACJA TESTOWA - TESTCONTAINERS (POSTGRESQL)
# ==================================================

# 1. Ten plik to konfiguracja profilu 'tc' (Testcontainers). Nie ustawiamy jawnie
# spring.datasource.driverClassName tutaj, poniewa? URL JDBC jest wstrzykiwany
# dynamicznie przez BaseIntegrationSpec (DynamicPropertySource). Gdyby?my ustawili
# driver na org.postgresql.Driver, a URL pozosta?by domy?lny (np. H2), powsta?by
# konflikt (postgres driver nie akceptuje URL H2). Dlatego driver zostaje wybrany
# automatycznie na podstawie docelowego jdbcUrl.

# 2. Zarz?dzanie schematem
spring.flyway.enabled=false

# Wy??cz automatyczne czyszczenie przy zamykaniu,
# Testcontainers i tak wywali ca?y kontener, wi?c nie musimy czy?ci? tabel
spring.jpa.hibernate.ddl-auto=create

# 3. Logi
spring.jpa.show-sql=true

# 4. Upewniamy si?, ?e konsola H2 jest wyl?czona
# spring.h2.console.enabled=false

